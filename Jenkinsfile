pipeline {
  agent any
    stages {
        stage('build') {
            steps {
                sh 'sudo apt install python-pytest'
                sh '12345678'
                sh 'python3 main.py'
            }
        }
    }
}